<%*
const types = [
  "생애최초매수", "단순매수", "1주택 매도", "다주택자매도",
  "갈아타기_매도측", "갈아타기_매수측", "보유관리", "전세"
];
const type = await tp.system.suggester(types, types, false, "케이스 타입 선택 (SCHEMA.md §6)");
const isTransactional = !["보유관리", "전세"].includes(type);

let status = "진행중";
if (isTransactional) {
  const statuses = ["관심단계", "임장중", "계약전", "계약완료", "잔금대기"];
  status = await tp.system.suggester(statuses, statuses, false, "진행 상태");
}

let linked = "null";
if (type.startsWith("갈아타기")) {
  const input = await tp.system.prompt("linked_case (짝 케이스 파일명, 예: case-002-과천매수)", "");
  if (input) linked = input;
}
%>---
type: <% type %>
status: <% status %>
key_dates: { 계약예정: null, 잔금예정: null }
linked_case: <% linked %>
관련_특례: []
---

# <% tp.file.title %>

<!-- SCHEMA.md §6 표에서 "<% type %>" 행의 필수 필드를 채울 것 -->
