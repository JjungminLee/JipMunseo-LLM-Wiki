# SCHEMA — JipMunSeo Wiki 컨벤션

이 문서는 에이전트가 `ingest`(원본 반영) / `query`(질의응답) / `plan`(케이스 체크리스트 생성) /
`notify`(개정 알림 팬아웃) / `lint`(정합성 점검)를 수행할 때 따라야 하는 규칙을 정의한다.

## 0. 대전제

1. `wiki/`, `raw/`, `index.md`, `SCHEMA.md`, `CHANGELOG.md` = **public**. 오픈소스로 공개되는 자산이며
   개인 식별 정보나 특정 케이스의 구체적 재무 정보가 절대 들어가면 안 된다.
2. `case/` = **private**. 본인(또는 이 레포를 clone한 각 사용자)의 profile·케이스·임장기록이며
   `.gitignore`로 커밋 대상에서 제외한다. 템플릿(`*.template`)만 public에 남는다.
3. `wiki/`는 "지금 맞는 말"이 아니라 **"몇 시행일 기준으로 맞는 말"**을 담는다. 개정되면 즉시 반영.
4. **법령 근거**와 **현장 의견(임장 통찰)**은 절대 같은 문장에서 섞이지 않는다. 항상 출처 종류를
   구분해서 인용한다.
5. **행정계획/고시**(국토교통부·지자체 등의 개발계획·노선 확정 등)는 법령·유권해석·판례와 달리
   **변경·취소될 수 있는 계획**이다. 확정된 법적 효력이 있는 것처럼 서술하지 않고, 항상 계획
   단계임을 명시한다(§3 인용 형식 참고).

## 1. 디렉토리 구조

```
raw/                        # public — 원본 archive, ingest 후 절대 수정하지 않음
  laws/                     # 법령원문 (법제처)
  rulings/                  # 판례
  interpretations/          # 유권해석 (국세청 등)
  notices/                  # 행정계획·고시 (국토교통부·지자체 등 — GTX 노선, 정비구역 지정 등)

wiki/                       # public — LLM이 raw/를 읽고 생성·갱신하는 위키 페이지
  <세목>.md                 # 예: 양도소득세.md, 종합부동산세.md, 취득세.md
  <지역/사업명>.md          # 지역개발계획 카테고리 — 예: GTX-A_노선.md

index.md                    # public — 전체 wiki 목차, 컨텍스트 윈도우에 통째로 들어갈 크기 유지
CHANGELOG.md                # public — 패치 이력 (semver, §9)

case/                       # private — .gitignore 대상 (템플릿 제외)
  profile.md                # 세대 구성, 보유주택 목록, 소득 등 느리게 변하는 사실
  case-index.md             # 진행 중/종료된 모든 케이스 목록 + 상태 + 연결관계
  cases/*.md                # 케이스별 파일
  archived/*.md             # 종료(closed)되거나 전환(전세→매수 등)된 케이스
  fieldnotes/*.md           # private — 임장 중 본인 관찰. 일반화되면 §6에 따라 wiki/로 승격
```

## 2. raw/ 소스 taxonomy

| 종류 | 위치 | 필수 메타데이터 |
|---|---|---|
| 법령원문 | `raw/laws/` | `공포일`, `시행일`, `조문번호`, `출처(법제처 URL)` |
| 판례 | `raw/rulings/` | `선고일`, `사건번호`, `법원` |
| 유권해석 | `raw/interpretations/` | `회신일`, `문서번호`, `발행기관(국세청 등)` |
| 행정계획/고시 | `raw/notices/` | `고시일자`(또는 `발표일자`), `고시번호`(있는 경우), `발행기관`, `계획단계`(고시확정/발표/추진중 등), `출처(원문 URL)` |

`raw/`는 ingest 시점에 저장된 그대로 보존한다. 개정되면 새 파일을 추가하고 기존 파일은 수정하지
않는다 — 이력 보존 자체가 목적이며, git log가 곧 개정 추적 파이프라인이다.

## 3. wiki/ 작성 컨벤션

**프론트매터 스키마** (notify 팬아웃과 plan 체크리스트가 이 필드를 사용한다):

```yaml
title: 일시적2주택 비과세
category: 양도소득세
관련_case_types: [갈아타기_매도측, 갈아타기_매수측]
관련_profile_fields: [보유주택목록, 세대원구성]
최종개정_시행일: 2026-01-01
source_refs: [raw/laws/소득세법시행령.md#104-2]
```

- **조문 인용 형식**: `[소득세법 §104-2③, 2026.1.1 시행]` — 항상 시행일을 붙인다. 공포일과 시행일이
  다르면 반드시 시행일 기준으로 서술하고, 괄호로 공포일을 병기한다.
- **위키링크**: 관련 개념/특례/케이스 유형은 `[[개념명]]`으로 연결한다. 존재하지 않는 링크도
  허용 — 나중에 채울 페이지의 자리표시자로 취급한다.
- **연혁 섹션**: 세율/요건이 개정되면 이전 내용은 본문에서 빠지고 페이지 하단 `## 연혁`으로
  이동한다. 본문에는 항상 최신 시행 기준만 남는다.
- **출처 구분 표기**: 법령 근거는 일반 서술로, 임장 통찰에서 승격된 내용이나 행정계획/고시
  기반 서술은 GitHub 호환 알림 콜아웃(`> [!TYPE] 제목` + 이어지는 `>` 인용줄)으로 감싸 법령과
  시각적으로 분리한다. 타입은 고정 3종만 쓴다 — Obsidian에서 타입별 자동 색상이 적용되고,
  GitHub에서도 네이티브 alert로 렌더링된다:
  - `> [!TIP] 🔎 현장 통찰` — 법적 근거는 없지만 임장/실거래에서 나온 인사이트
  - `> [!NOTE] 📋 행정계획 (변경·취소 가능)` — 확정 법령이 아닌 계획·고시 기반 서술
  - `> [!WARNING] ⚠️ 해석 상충` — 우선순위로 해결 안 되는 판례/유권해석 충돌 (§4)
- **행정계획/고시 인용 형식**: `[국토교통부 고시 제2026-123호, 2026.3.1 발표]`처럼 발행기관·
  고시번호(있으면)·발표일을 함께 표기한다. 노선·사업 계획은 착공/개통 등 확정된 사실처럼
  쓰지 않고, 계획 단계(예정·추진중·고시확정 등)를 항상 병기한다.
- **지역개발계획 프론트매터**: 법령 기반이 아니므로 `최종개정_시행일` 대신 `최종확인일`(가장
  최근에 진행 상황을 재확인한 날짜)을 쓴다. `source_refs`는 `raw/notices/*.md`를 가리킨다.

## 4. 충돌 처리 규칙

소스 간 내용이 상충하면 다음 우선순위로 판단한다:

1. 법령원문 (최신 시행일 우선)
2. 유권해석
3. 판례
4. 현장 통찰 (승격된 fieldnote)

동일 우선순위 내에서는 **최신 시행일/발행일**이 우선한다. 우선순위로 해결이 안 되는 모호한
충돌(예: 하급심과 상급심 판례가 다른 논리를 씀)은 위키 페이지에 두 입장을 병기하고
`> [!WARNING] ⚠️ 해석 상충` 블록으로 명시한다 — 임의로 하나를 정답처럼 서술하지 않는다.

## 5. fieldnote → wiki 승격 규칙

`case/fieldnotes/*.md`는 private이며 특정 케이스(주소, 시기 등 구체적 정보)와 묶여 있다.
아래 조건을 만족하면 wiki/로 승격 후보가 된다:

1. 같은 패턴이 **2회 이상** 다른 시점/장소에서 관찰됨, 또는
2. 법령·제도와 직접 연결되어 다른 사용자에게도 일반적으로 유효한 통찰일 것

승격 시 체크리스트:
- [ ] 구체적 주소·동호수·거래 상대방 등 PII 제거, 지역명은 "OO동" 수준으로만 일반화
- [ ] 본인 재무 정보(매수가, 대출액 등) 제거
- [ ] `> [!TIP] 🔎 현장 통찰` 블록으로 감싸 법령 서술과 분리
- [ ] 원본 fieldnote 파일에 `승격됨: wiki/<파일>.md#<섹션>` 역참조 기록 (private 쪽에서만 추적)

## 6. 케이스 타입 taxonomy

거래형(시작·종료가 있어 결국 `closed`) / 상태형(상시 유지, `closed` 없음)으로 나눈다.

### 거래형

| 타입 | 정의 | 필수 필드 | 자동 매핑 wiki | 특이사항 |
|---|---|---|---|---|
| `생애최초매수` | 세대원 전원 생애 첫 주택 취득 | 매수가, 청약통장 가입기간, 소득요건 | [[생애최초 취득세 감면]] [[디딤돌대출]] [[생애최초 특별공급]] | 취득세 감면 가액상한·무주택 요건이 핵심 분기 |
| `단순매수` | 매도 계획 없이 신규/추가 주택 취득 | 매수 후 총 보유주택수, 조정대상지역 여부 | [[취득세 중과]] [[LTV 지역별 규제]] | 매수 후 몇 번째 주택인지가 취득세율을 가름 |
| `1주택 매도` | 갈아타기 없이 1주택자가 매도만 함 | 보유기간, 거주기간, 실거래가 | [[1세대1주택 비과세]] [[장기보유특별공제]] | linked_case 없음, 특례기간 제약 없음. 고가주택(12억 초과) 과세분 별도 계산 |
| `다주택자매도` | 2주택 이상 보유자가 그중 하나 매도 | 매도대상 조정지역 여부, 보유기간, 중과배제특례 해당여부 | [[양도세 중과]] [[다주택자 중과배제 특례]] [[분양권·입주권 양도세]] | 중과 여부가 세율 최대 분기점 |
| `갈아타기_매도측` | 종전주택 매도 (매수측과 세트) | 종전주택 취득일, 신규주택 계약/취득 예정일 | [[일시적2주택 비과세]] [[1세대1주택 비과세]] | `linked_case` 필수. 매수측과 항상 함께 재평가 |
| `갈아타기_매수측` | 신규주택 매수 (매도측과 세트) | 신규주택 계약일, 잔금예정일, 종전주택 매도예정일 | [[일시적2주택 비과세]] [[취득세 일시적2주택]] [[LTV·DTI·DSR]] | `linked_case` 필수. 처분기한 준수가 취득세 중과배제 조건 |

### 상태형

| 타입 | 정의 | 필수 필드 | 자동 매핑 wiki | 특이사항 |
|---|---|---|---|---|
| `보유관리` | 매수/매도 없이 보유 주택의 세금·규제만 관리 | profile.md 보유주택 목록 연동, 공시가격 | [[종합부동산세]] [[재산세]] [[임대주택사업자 등록]] | 공시가격 발표 시 재평가 트리거 |
| `전세` | 무주택 임차인 상태 (매수 준비 전 단계) | 전세보증금, 계약 갱신일/만기일, 대출여부 | [[전세자금대출]] [[전세보증보험]] [[임차인 대항력·확정일자]] | 청약가점(무주택기간) 산정에 영향. §8 전환 규칙 참고 |

**케이스 프론트매터 공통 스키마:**

```yaml
type: 갈아타기_매도측
status: 계약전            # 거래형: 관심단계/임장중/계약전/계약완료/잔금대기/closed
                          # 상태형: 진행중 (고정)
key_dates: { 계약예정: null, 잔금예정: null }
linked_case: case-002-과천매수   # 갈아타기 타입만 필수
관련_특례: [[일시적2주택 비과세]]
```

## 7. linked_case 재평가 규칙

- `갈아타기_매도측` / `갈아타기_매수측`은 반드시 쌍으로 존재해야 한다. `linked_case`가 비어 있으면
  `lint` 단계에서 경고한다.
- 연결된 케이스 쌍과 관련된 wiki 페이지(예: [[일시적2주택 비과세]])가 갱신되면, 두 케이스를
  **항상 함께** 재평가한다 — 한쪽만 보고 판단하지 않는다.

## 8. 케이스 라이프사이클

- **거래형 종료**: 잔금일이 지나면 `status: closed`로 전환하고 `case/archived/`로 이동. 동시에
  `profile.md`의 보유주택 목록을 이 사실로 갱신한다.
- **전세 → 매수 전환**: `생애최초매수`/`단순매수` 케이스가 `계약완료` 상태가 되는 시점에, 열려있던
  `전세` 케이스는 삭제하지 않고 `status: closed`, `전환_to: <신규 매수 케이스 ID>`를 기록한 뒤
  `case/archived/`로 이동한다. 종료일(=계약일)은 `profile.md`의 `직전_무주택기간_종료일`에
  반영한다 — 무주택기간 이력은 향후 재매도/재매수 시에도 끊기면 안 된다.
- **보유관리 / 전세(미종료)**: 상태형이므로 자동 종료되지 않는다.

## 9. 버전/패치 규칙

`CHANGELOG.md` + git tag로 관리하며 semver를 따른다:

- **major**: 디렉토리 구조·SCHEMA.md 자체의 규칙 변경
- **minor**: 세율/요건 등 제도 개정 반영 (wiki 본문 실질 변경)
- **patch**: 오탈자, 표현 수정, 링크 보정 등 실질 내용 변화 없는 수정

커밋 메시지는 "시행일 / 무엇이 바뀌었는지"를 담는다. 예: `2026.1.1 시행 / 양도세 세율구간 조정`.
이 커밋 메시지 자체가 개정 이력이므로, 별도 diff 파이프라인을 만들지 않는다.

## 10. notify 팬아웃 로직

```
wiki 페이지 갱신
  → 프론트매터의 관련_case_types / 관련_profile_fields 확인
  → case-index.md 순회하며 해당 타입/필드를 가진 케이스 탐색
  → 매칭되면 해당 케이스에 "영향 있음" 로그 추가
     → linked_case가 있으면 두 케이스를 §7 규칙에 따라 함께 재평가
  → 매칭 안 되면 알림 생략 (LLM 호출 자체를 태그 매칭으로 사전 필터링)
```

## 11. lint 체크리스트

주기적으로 다음을 점검한다:

- [ ] wiki 페이지가 raw/ 원본과 어긋나지 않는가 (왜곡 검증)
- [ ] 어느 wiki 페이지도 링크하지 않는 고아 페이지가 있는가
- [ ] `최종개정_시행일`이 최신 raw/ ingest보다 오래된 페이지가 있는가
- [ ] `갈아타기_*` 타입인데 `linked_case`가 비어 있는 케이스가 있는가
- [ ] public 대상 파일(`wiki/`, `index.md`, `raw/`)에 PII·구체적 재무정보가 섞여 있는가
- [ ] `case/fieldnotes/`에 §5 승격 조건을 만족하는데 아직 승격 안 된 항목이 있는가
