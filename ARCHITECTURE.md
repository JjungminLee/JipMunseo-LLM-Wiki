# JipMunSeo — 레이어 아키텍처

부동산 정책/세법 위키 에이전트라는 기획 의도에서 나온 세 가지 제약을 아키텍처의
기준으로 삼았습니다.

1. **법령·정책은 매년, 때로는 수시로 바뀐다** → 답변은 "지금 맞는 말"이 아니라
   "몇 시행일 기준으로 맞는 말"이어야 하고, 개정되면 즉시 반영할 수 있는 파이프라인이 필요.
2. **임장/실거래 인사이트는 법령과 성격이 다르다** → 법적 근거는 없지만 가치 있는
   정보이므로, 절대 법령과 섞여서 "마치 법인 것처럼" 답변에 섞이면 안 됨.
3. **AI 답변은 근거를 인용할 수 있어야 신뢰할 수 있다** → RAG 검색 결과와 최종 답변이
   항상 출처(법령 조항 vs 현장 의견)를 달고 다녀야 함.

이 세 가지가 레이어 경계를 어디에 그을지를 결정했습니다.

```
┌─────────────────────────────────────────────────────────┐
│ 1. Presentation Layer  (frontend/, backend/app/api)       │
│    React UI ↔ FastAPI 라우터. HTTP 계약만 담당, 로직 없음   │
├─────────────────────────────────────────────────────────┤
│ 2. Application Layer  (backend/app/services)               │
│    유스케이스 조합: "질문에 답하기", "위키 항목 조회하기"    │
│    라우터는 이 계층만 호출 — infra/db를 직접 만지지 않음     │
├─────────────────────────────────────────────────────────┤
│ 3. Agent / LLM Layer  (backend/app/agent)                  │
│    LLM 호출을 담당하는 유일한 계층. 시스템 프롬프트가        │
│    "법령 근거"와 "현장 의견"을 분리해서 인용하도록 강제        │
│    도구(tool) 예: 양도세 추정 계산기                         │
├─────────────────────────────────────────────────────────┤
│ 4. Retrieval Layer  (backend/app/retrieval)                │
│    RAG 검색. LegalRule 청크와 FieldInsight 청크를 별도로     │
│    태깅해서 반환 → 에이전트가 출처 종류를 알고 인용 가능      │
├─────────────────────────────────────────────────────────┤
│ 5. Domain Layer  (backend/app/domain)                      │
│    핵심 엔티티: LegalRule(시행일 버전 관리), FieldInsight,   │
│    WikiEntry(위 둘을 조합해 사용자에게 보여주는 문서)          │
├─────────────────────────────────────────────────────────┤
│ 6. Ingestion / ETL Layer  (backend/app/ingestion)           │
│    법제처·국세청·국토부 등 출처별 크롤러 → 변경분만 diff     │
│    → LegalRule 갱신 + 벡터 재임베딩. 스케줄 실행(APScheduler) │
├─────────────────────────────────────────────────────────┤
│ 7. Infra Layer  (backend/app/infra)                         │
│    구조화 DB(Postgres: LegalRule/FieldInsight/WikiEntry),   │
│    벡터 DB(Qdrant), LLM 클라이언트. 외부 시스템과의 유일한 접점 │
└─────────────────────────────────────────────────────────┘
```

## 계층별 책임과 이유

| 계층 | 위치 | 책임 | 왜 분리했는가 |
|---|---|---|---|
| Presentation | `frontend/`, `backend/app/api` | HTTP 요청/응답, 화면 렌더링 | UI 프레임워크 교체가 로직에 영향 없도록 |
| Application | `backend/app/services` | 유스케이스 오케스트레이션 | 라우터가 두꺼워지는 걸 막고, API 계약과 비즈니스 로직을 분리 |
| Agent/LLM | `backend/app/agent` | 프롬프트, 도구 호출, LLM 응답 조합 | LLM 관련 로직을 한 곳에 모아야 "법령 vs 현장 의견 분리 인용" 같은 규칙을 한 군데서만 강제할 수 있음 |
| Retrieval | `backend/app/retrieval` | 벡터 검색, 청크 랭킹 | 임베딩 모델/벡터 DB를 바꿔도 에이전트 로직은 그대로 |
| Domain | `backend/app/domain` | LegalRule/FieldInsight/WikiEntry 엔티티 | 법령은 "시행일 버전"이 있고 인사이트는 없다는 차이를 타입으로 표현 |
| Ingestion | `backend/app/ingestion` | 출처별 크롤러, 변경 감지, 재색인 | 법이 바뀌는 빈도(연 수 회)와 앱 배포 빈도가 다르므로 별도 스케줄 잡으로 분리 |
| Infra | `backend/app/infra` | DB/벡터DB/LLM 클라이언트 | 실제 외부 연결은 여기서만, 나머지 계층은 인터페이스만 앎 |

## 지금 만들어진 것 / 아직 안 만들어진 것

- ✅ 레이어 스캐폴딩, 도메인 모델, API 라우터, 에이전트 오케스트레이터 골격
- ❌ 실제 DB 스키마/마이그레이션, 크롤러 구현, 벡터 검색 연결 — 전부 `TODO`/`NotImplementedError`로 표시해둠. Postgres·Qdrant를 붙이고 첫 크롤러(예: 법제처)를 구현하는 게 다음 단계.
