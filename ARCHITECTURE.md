# JipMunSeo — 아키텍처

부동산 정책/세법 위키 에이전트라는 기획 의도에서 나온 제약을 아키텍처 기준으로 삼았다.

1. **법령·정책은 매년, 때로는 수시로 바뀐다** → 답변은 "지금 맞는 말"이 아니라 "몇 시행일 기준으로
   맞는 말"이어야 하고, 개정되면 즉시 반영할 수 있는 파이프라인이 필요.
2. **임장/실거래 인사이트는 법령과 성격이 다르다** → 법적 근거는 없지만 가치 있는 정보이므로, 절대
   법령과 섞여서 "마치 법인 것처럼" 답변에 섞이면 안 됨.
3. **이 에이전트는 처음부터 끝까지 한 사람(본인)을 위한 도구다** → 여러 사용자를 다루는 서비스가
   아니므로 인증·멀티테넌시·DB 인프라가 필요 없고, Git 저장소 하나로 충분하다.
4. **위키 지식은 다른 사람에게도 유용할 수 있다** → 세법/정책 위키(`wiki/`)는 오픈소스로 공개하되,
   개인 재무·거주 정보(`case/`)는 물리적으로 분리해서 로컬/비공개로만 존재해야 한다.

## 왜 RAG가 아니라 LLM Wiki(Karpathy 패턴)인가

개인 지식베이스 스케일(수만~10만 단어대)에서는 `index.md`를 통째로 컨텍스트에 넣고, 거기서 관련
페이지 2~4개만 골라 읽는 방식이 벡터DB·임베딩 파이프라인보다 관리 부담과 정확도 양쪽에서
유리하다. 이 스케일을 넘어서면(문서량이 index.md를 컨텍스트에 다 못 넣을 만큼 커지면) 그때
가벼운 키워드 검색(BM25) 정도만 index.md 검색용으로 보강한다 — 임베딩/벡터DB는 여전히 불필요.

## 레이어

```
raw/        원본 archive (법령·판례·유권해석). ingest 후 수정하지 않음. public.
wiki/       LLM이 raw/를 읽고 생성·갱신하는 위키 페이지. [[위키링크]]로 상호 참조. public.
index.md    전체 wiki의 목차/지도. 쿼리 시 항상 먼저 읽는 진입점. public.
SCHEMA.md   인용 형식, 충돌 처리, 케이스 taxonomy 등 컨벤션 정의. public.
case/       profile·케이스·임장기록. 본인(또는 각 사용자)의 실제 상황. private.
```

세부 규칙(디렉토리 구조, 케이스 타입, 승격 규칙, 라이프사이클 등)은 전부 [SCHEMA.md](SCHEMA.md)에
정의되어 있다 — 이 문서가 아키텍처의 "왜"를, SCHEMA.md가 "어떻게"를 담당한다.

## 다섯 가지 동작

| 동작 | 설명 |
|---|---|
| `ingest` | 새 raw/ 소스를 넣으면 관련 wiki 페이지들을 갱신 |
| `query` | index.md → 관련 wiki 페이지 → 답변 |
| `plan` | 케이스 타입을 보고 관련 wiki 페이지를 끌어와 체크리스트 생성 |
| `notify` | wiki 갱신이 case-index.md의 어떤 케이스에 영향 있는지 팬아웃 (SCHEMA.md §10) |
| `lint` | 원본 대조, 고아 페이지, 오래된 시행일 참조, PII 유입 등 주기 점검 (SCHEMA.md §11) |

## 공개(오픈소스) 배포와 개인정보 경계

`wiki/`, `raw/`, `index.md`, `SCHEMA.md`, `CHANGELOG.md`는 공개 레포로 유지한다. 개정 반영은
git tag로 패치 버전을 매기고(SCHEMA.md §9), GitHub Release/Watch로 다른 사용자에게 전파된다 —
별도 알림 서버가 필요 없다. `case/`는 `.gitignore`로 완전히 분리되어(`case/README.md` 참고) 이
레포를 clone한 누구도 서로의 재무·거주 정보를 공유하지 않는다. 즉 "타인의 민감정보를 중앙에서
다루는" 상황 자체가 구조적으로 발생하지 않는다.

## 지금 만들어진 것 / 아직 안 만들어진 것

- ✅ `raw/`, `wiki/`, `case/`, `index.md`, `SCHEMA.md`, `CHANGELOG.md` 스캐폴딩
- ❌ 실제 ingest/query/plan/notify/lint 에이전트 프롬프트, 첫 raw/ 소스(예: 법제처 크롤링), wiki
  페이지 실제 작성 — 다음 단계.
